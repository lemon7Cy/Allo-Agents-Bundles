---
name: dfcode-usage-analysis
description: 用 DFCode 企业 MCP 做用量分析时的方法论——部门/人员 token 用量、时间段对比、"为什么涨/跌"的根因拆解,以及答到用户粒度的用量下降预警。当用户问"部门用量""谁用得多""这几天为什么下降/上升""对比两个周期",或(在飞书群 @机器人)问"谁在掉量""用户用量下降""掉量预警/用量下降提醒"等,先读本 skill。
---

# DFCode 用量分析方法论

当用户问企业/部门/人员的 **用量、排名、趋势、对比、"为什么变了"** 时,按本方法论分析。数据来自 DFCode 企业 MCP(`query_departments` / `query_employee_detail` / `query_roster` / `query_dashboard` 等)。

**目标:让看的人一眼拿到结论,而不是一张要自己找答案的大表。**

## ⚠️ 查询预算铁律(最优先 —— 不守会把整个会话撑爆报错)

dfcode 工具一次能返回**巨量**数据,多调几次就把上下文堆爆、整条会话直接报 "internal error"(真发生过:全员逐个拉明细 → 单轮 1.45MB / ~43 万 token → 模型拒答)。所以:

- **聚合优先**:先用 `query_departments`(一次就带 daily trends / WoW growth / model breakdown / top employees per dept)拿全局,**别上来就逐人拉**。
- **逐人查严格限量**:`query_employee_detail` / `query_department_employee_detail` **只对筛出来的 3–5 个关键人**调,**绝不循环全员**(全员逐个 = 必爆,这就是上次崩的原因)。
- **不要 raw / 全量明细**:要趋势就用 daily trends(已按天汇总),不要 paginated raw entries。
- **一次分析总工具调用 ≤ 8 次**;还不够就说明你在硬拉明细,停下来改聚合,而不是继续逐人。
- 经验:单个工具结果别超过 ~1.5 万 token。

## 0. 第一原则:先结论,后证据

- **第一行/第一句就回答用户的问题**(涨还是跌、为什么、主因是谁),再给支撑数据。
- 不要先甩 Top5 大表让用户自己看。**表格是证据,结论在前。**

## 1. 数据口径(最重要,错了全错)

- **未分配部门的人 = 非公司所属人员,直接排除。** 汇报里**不统计、不排名、不出现、也不要建议"为他们确认部门归属"** —— 他们就是噪声,当作不存在。
- **只统计已分配部门的人员。** 判定归属用 `query_roster`(按部门/状态)或 `query_departments` 的部门维度;`department=null` / 无部门 / 编外状态的人一律剔除。
- 默认**按部门聚合**;需要时再下钻到具体人(也只在已分配部门内)。
- 在结论末尾用一行声明口径,例:`口径:仅已分配部门(已排除未分配人员)。用量以 token 计。`
- ⚠️ 不要因为某未分配的人用量很高,就把他列出来或建议补归属 —— **高用量但未分配 = 仍然排除**,顶多在缺口里用一句概括"另有未分配人员用量未纳入",不点名。

## 2. 指标定义:用量 = token,不是请求次数

- **排名、对比、"高用量用户"一律以 token 消耗为准。**
- **请求次数 ≠ 用量。** 请求多但 token 低 = 高频小请求(可能是脚本/轮询/补全场景),**不是高用量用户**。
- 请求次数只在解释"调用模式"时作为辅助信号提一句,**绝不作为用量排名依据**,默认不在结论里突出展示。
- 如果某人请求次数高但 token 很低,要**主动点出**这是"高频低耗",纠正"他是高用量用户"的误判。

## 3. 对比方法论:控制变量(组长重点踩过的坑)

做任何对比前,**先问一句:我在控制什么变量?** 只允许两种干净的对比,**绝不混维度**:

- **纵向(锁对象,看时间)**:同一个部门 / 同一个人 / 同一个模型,在不同时间段的变化。例:智能视迅部门 本月 vs 上月 token。
- **横向(锁时间,比对象)**:同一个时间段内,不同部门 / 不同人 / 不同模型 之间的高低。例:本月各部门 token 排名。

**禁止的脏对比(一动就动了两个变量,会被一眼看穿):**
- 把"周期A 第 1 名"和"周期B 第 1 名"并排比——他们往往是**不同的人**(对象和时间一起变了),无意义。
- 拿"部门总量"和"某个人用量"放一起比——**粒度不一致**。
- 混口径比:token vs 请求次数、含编外 vs 不含编外、不同长度的时间窗。

**要看"人随时间"**,就**取两个周期的并集人群,逐人**列 `token(A) → token(B) → Δ`(对象固定=每个人,只动时间这一个变量)。工具用 `query_departments` 的 daily trends / WoW growth / top employees per dept。

## 4. 根因拆解:直接回答"为什么涨/跌"

当部门用量变化时,按这个链路给答案:

1. **结论先行**:部门 token 环比 涨/跌 X%(从 A 到 B),主因是 ___。
2. **拆解到人/模型**:对比同部门两个时间段,找出 **token 变化最大的几个人 / 哪个模型迁移了**。
3. **区分两种情况**(组长想知道的就是这个):
   - **主力轮换**:部门总量基本稳定,只是活跃的人换了一批 → 说清"不是真下降,是 X 换成了 Y"。
   - **真实下降**:总量实质减少 → 找出**是谁停用/降了**(用 `query_employee_detail` 看其 daily trend 佐证),以及是否伴随**配额变化、模型下线、离岗**。
4. **可证伪**:数据缺失就说缺失(例:周期太短、某天数据不全),不要硬编因果。

## 5. 工具速查

| 你要做的事 | 用哪个工具 |
|---|---|
| 部门用量 / 对比 / 趋势 / WoW / 部门内 top | `query_departments`(带 range filters / daily trends / WoW / top employees per dept) |
| 单人深挖(概览、日趋势、模型分布、明细) | `query_employee_detail` / `query_department_employee_detail` |
| 部门归属 / 编外判定 / 按状态筛 | `query_roster` |
| 全局概览(今日/7d/月、活跃人数、模型分布) | `query_dashboard` |
| 跨维度聚合 | `query_usage` |

## 6. 输出模板(部门用量对比类问题)

```
结论:<部门> 近 <周期> token 环比 <涨/跌 X%>,主因是 <主力轮换 / 谁降了 / 谁升了>。

变化拆解(同部门,同口径):
- <人/模型A>:<tokenA> → <tokenB>（Δ<±X>）
- <人/模型B>:...
（只列 token 变化最大的 3-5 项;请求次数如异常另起一行点出"高频低耗"）

判断:<主力轮换 / 真实下降>，依据 <…>。
口径:仅已分配部门，排除未分配/编外人员；用量以 token 计。
缺口:<数据不全/周期过短等，如有>
```

## 7. 用户用量下降预警(被问到时，答到用户粒度)

场景:星元接飞书机器人后，有人在群里 **@机器人** 问"谁的用量在掉 / 用户用量下降 / 掉量预警"。这是**被问才答**(不定时主动推),但要答到**单个用户粒度**——指出"谁在掉、掉多少、像不像真流失"。回复由飞书渠道直接发回 @ 的那个群(无需额外上报;只有用户明确要求发到**别的群**时,才用 `feishu-webhook-report`)。

### 7.1 怎么定位"在掉量的人"(两步，省 MCP 调用)
1. **批量筛候选**:`query_departments`(WoW growth / daily trends / top employees per dept)或 `query_dashboard` 一次拿到按人/部门的"近周期 vs 上周期"token，圈出环比下降明显的人,**别逐人拉**。
2. **逐人证实**:对候选用 `query_employee_portal_stats`(单人日趋势 + 365 天活跃热力图)/ `query_employee_detail` 看日趋势,确认是"持续/显著下降"还是单日波动。

### 7.2 两类掉量,分开看(别只盯大用户)
掉量有两种,**关注点不同,两类都要报**——口径沿用第 1、2 节(只看已分配部门、用量 = token):

**A · 用量影响(大用户掉了,影响 token 大)**
- **环比骤降**:本周期 token 比上周期跌 ≥ 50%。
- **持续下滑**:连续 ≥ 3 个周期 / N 天单调下降。

**B · 活跃转化(用量不大甚至很小,但"没用起来 / 不用了")** —— 影响的是活跃和留存转化,**不能因为 token 小就漏掉**。
- **由活转静**:之前有稳定使用,近 7 天 token 趋近 0(不看跌幅 %,看"还用不用")。
- **新用户 / 低基数静默**:历史用量本就很低甚至几乎不用,且连续 ≥ 7 天无 token —— 基数太小算不出有意义的跌幅 %,**按"有没有用起来"判断,照样列出**(尤其近期新接入 / 重点推广对象)。

> ⚠️ **关键:不要因为某用户绝对 token 小,就把他从名单里剔除。** 大用户走 A(看用量影响),小 / 新用户走 B(看活跃转化)。两类阈值都可调。

### 7.3 先排除"假下降"(关键,别误判成流失)
答之前用工具排掉这些,否则结论会被一眼看穿:
- **主力轮换**(第 4 节):部门总量稳、只是换了活跃的人 → 不是个人掉量。
- **离岗/编外/转岗**:`query_roster` / `query_user_detail`(roster status)→ 离职转岗的标注即可,不算预警。
- **配额/模型变化**:`query_user_detail`(MAAS quota)、`query_maas_models` / `query_model_endpoints`(模型下线)→ 配额被砍 / 常用模型下线导致的掉量,要**标因**,不是用户主动不用了。
- **请假/节假日/周末**:周期太短或跨假期,用 365 天热力图看季节性,别把放假当流失。

### 7.4 分级(A、B 两类各自分级)
| 级别 | A 用量影响 | B 活跃转化 |
|---|---|---|
| 🔴 预警 | 高用量用户骤降 ≥ 50% / 由活转静,已排假下降 | 新接入 / 重点推广对象静默 ≥ 7 天 |
| 🟡 关注 | 持续小幅下滑 / 未到阈值 → 下周期复核 | 低基数用户静默(非重点对象)→ 列名单、下周期复核 |
| ⚪ 不算 | 命中假下降(轮换/离岗/配额/节假日) | 确属正常(一次性试用、已离岗等) |

### 7.5 回复格式(结论先行,适配飞书卡片)
```
结论:近 <周期>，用量影响类 <N> 人明显下降,活跃转化类 <K> 人静默/未激活。口径:仅已分配部门,token 计,已排除离岗/配额/节假日等假下降。

🔴 用量影响 · 骤降/由活转静:
- <姓名/工号·部门>:<上周期 token> → <本周期 token>（Δ-X%），疑因 <真降 / 未知>，建议 <跟进动作>。

🟡 活跃转化 · 静默/未激活(低基数也算):
- <姓名·部门>:历史仅 <总量 token>，<最后使用日> 后静默 → 疑似没用起来,建议确认登录/权限/使用场景。

缺口:<数据不全 / 周期过短 / 某人离岗待确认 等>
```

### 7.6 触发与默认行为
- 群里 @机器人 问"谁在掉量 / 用户用量下降 / 掉量预警 / 用量下降提醒"→ 走本流程,**答到用户粒度**。
- **被问才答**;回复默认发回当前群(飞书渠道自动处理),**不主动定时推送**。

## 8. 深度用量分析 · 标准流程(SOP)

当被要求**多维深度分析**(例:"分析某部门本月工作日整体用量趋势,挑出谁持续增长/谁突然下降,各人模型使用也要趋势分析,对比粒度要一致")时,**严格按本流程走**——高效、粒度一致、且守住「查询预算铁律」不撑爆。

**Step 1 · 锁范围 + 一次聚合拿底座(关键省调用)**
- 先确认四件事:**部门、周期、是否只看工作日、口径(默认 token)**。
- **一次** `query_departments`(filter=该部门,range=该周期,带 `daily trends` + `WoW growth` + `model breakdown` + `top employees per dept`)→ 一口气拿到:① 部门按天总量 ② 部门内各人周期对比 ③ 模型分布。**别用逐人循环代替这一步。**
- **只看工作日**:从 daily trends 里**剔除周六周日**(及已知节假日)再算,避免周末噪声混进趋势。

**Step 2 · 整体趋势(结论先行)**
- 用"仅工作日"的 daily trends 给部门本月走势:上升 / 平稳 / 下降 + 环比 X% + 关键拐点(哪天起变的)。第一句就是结论。

**Step 3 · 分人分类(纵向:锁人看时间)**
- 用 Step 1 各人的周期序列,按"工作日 token 随时间"分两类(沿用第 7 节信号 + 排假下降):
  - **持续增长**:工作日序列基本单调上升 / 正斜率。
  - **突然下降**:某工作日起骤降 / 由活转静。
- 只列分类需要的人,别全员铺开。

**Step 4 · 关键人的模型趋势(硬上限 3–5 人)**
- 只对 Step 3 挑出的**增长最猛 + 下降最猛的少数人(各 ≤ 3)**,`query_employee_detail`(`daily trend` + `model distribution`)→ 看其**模型使用随时间怎么变**(换了模型?某模型涨/停?)。
- **绝不**对全员做这步(这就是撑爆红线)。

**Step 5 · 粒度一致校验(对应第 3 节,必做)**
- 所有对比锁**同一周期、同一"仅工作日"口径、同一 token 计、同一批人**;
- 纵向就锁人看时间,横向就锁时间比人,**绝不混维度**。

**Step 6 · 结构化输出**
```
结论:<部门> 本月工作日用量 <升/降 X%>,主因 <…>。口径:仅工作日、token 计、本月。

整体趋势:<按周/按天一句话 + 关键拐点>

📈 持续增长(N 人):
- <姓名>:<周期首 token> → <周期末 token>（Δ+X%），模型:<主用模型 + 变化趋势>
📉 突然下降(M 人):
- <姓名>:<最后活跃工作日> 后骤降/静默,疑因 <真降 / 换人 / 配额 / 请假>，模型:<…>

粒度声明:本节所有对比同口径(仅工作日、token、本月、同一批人)。
缺口:<数据不全 / 某人离岗待确认 等,如有>
```

> 自检:这套流程**总工具调用应在 ~5–8 次**(1 次聚合 + 3–5 次关键人)。如果你发现自己在 `query_department_employee_detail` 上逐个员工循环,**立刻停**,回到 Step 1 的聚合口径。
